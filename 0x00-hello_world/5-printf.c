#include <stdio.h>

/*
 * main - Initiates execution of programme file
 */
int main(void)
{
	/**
	 * print f - Prints a string of characters to the standard output
	 */
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
